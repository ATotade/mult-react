sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t atotade/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true stephengrider/react-test npm test

after_success:
  - docker build -t atotade/multi-client ./client
  - docker build -t atotade/multi-nginx ./nginx
  - docker build -t atotade/multi-server ./server
  - docker build -t atotade/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push atotade/multi-client
  - docker push atotade/multi-nginx
  - docker push atotade/multi-server
  - docker push atotade/multi-worker
